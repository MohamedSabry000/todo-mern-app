export type Todo = {
  id: string;
  title: string;
  description: string;
  priority: string;
  status: string;
  startDate: string;
  endDate: string;
}

export type User = {
  id: string;
  name: string;
  email: string;
  password: string;
}